let tutorNames = ["최원장", "김르탄", "윤창식", "박가현", "김병연", "내배캠"];

tutorNames.forEach(function (name) {
  if (
    name === "최원장" ||
    name === "윤창식" ||
    name === "박가현" ||
    name === "김병연"
  ) {
    console.log(name);
  }
});
